- hosts: web
  become: yes
  roles:
    - nginx_role

- hosts: web
  become: yes
  tasks:
    - name: Call custom nginx module
      nginx_port:
        port: 8080
- hosts: web
  become: yes
  tasks:
    - name: Configure nginx port via custom module
      nginx_port:
        port: 8080

